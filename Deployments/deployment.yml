apiVersion: apps/v1
kind: Deployment
metadata:
  name: twitterclone
  labels:
    app: twitterclone
spec:
  replicas: 1
  selector:
    matchLabels:
      app: twitterclone
  template:
    metadata:
      labels:
        app: twitterclone
    spec:
      containers:
      - name: twitteruserservice
        image: semm12/twitteruserv2
        imagePullPolicy: Always
        ports:
          - containerPort: 8090
